import { Button } from "@headlessui/react";

export default function ShareButton() {
  return (
    <button className="px-4 py-2 bg-gradient-to-r from-blue-500 to-cyan-400 text-white font-extrabold rounded-lg shadow-[0_0_12px_2px_rgba(0,255,255,0.5)] hover:from-cyan-400 hover:to-blue-500 transition-all duration-200 uppercase tracking-wider">
      Share
    </button>
  );
}

  